# Проверка тестового API, 18.09.2026

Функция: sign-storage-api-test (`d4emlmsp7hd8gr65scac`).
Проверенная версия: `d4eed65e5j82lvfj76pf`.
Рабочие функции sign и bukupl не обновлялись.

## Реальный S3

Фактический бакет bukupl: `1bf11148-3595-4a07-a089-d460153b7c7a`.
Папка `Задания/`: 636 объектов, две страницы по 500/136 объектов.
Время ответов списка: 1.447 и 0.682 с (один прогон, не SLA).
Прочитаны теги первых 20 объектов.

Успешно проверены: отказ без ключа/с неверным ключом; capabilities;
запрет очереди в тестовом окружении; запрет записи вне тестового префикса;
загрузка нового JSON; отказ 412 при повторной загрузке с If-None-Match;
добавление и удаление отдельного тега с сохранением другого тега;
просмотр по подписанной ссылке с inline и text/plain, содержимое совпало.

Сохранён тестовый объект:
`_api-tests/20260918/probe-cc9005fa-8cab-42f6-835e-80108b2d5639.json`.
При первоначальной проверке в бакете из устаревшего README (20ab2a0c-2726-4ba1-9c7c-7deae82941ff)
также сохранён `_api-tests/20260918/probe-e3b5284b-7622-40d3-9595-2f69b8e6077d.json`.
Это новые безвредные JSON-пробы; рабочие объекты не изменялись, ничего не удалялось.

## Локальные проверки

5 тестов серверного контракта, включая старые ReceiveMessage/S3Links и DeleteMessage.
21 тест интерфейса: 20 строк на странице, переходы, кеш, изменение только одной строки,
фильтры до загрузки тегов, ошибки отдельных объектов, таймаут SignJS, смена папок.
В Chromium-Gost и встроенном браузере визуально проверены модуль SignJS и страницы
по 20 строк на явно обозначенных тестовых данных. Во встроенном браузере нет
плагина КриптоПро; модуль показывает ошибку плагина. В Chromium-Gost модуль загружен.

Реальная подпись документов и рабочая очередь не запускались. Полный цикл подписи
нужно проверять отдельно на тестовой очереди перед выпуском в рабочую функцию.
Реальная работа UI с API требует ввода существующего ключа SignJS в настройках
локального origin; ключ не включён в исходники и URL.
