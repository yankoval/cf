# Выпуск 22.09.2026

Рабочая версия sign: `d4e76o4n441ndcirh286`.
Основной API: https://d5d6kfk1rvhoeodarkrj.p8361f8z.apigw.yandexcloud.net
Основная страница: https://storage.yandexcloud.net/20ab2a0c-2726-4ba1-9c7c-7deae82941ff/index.html

Gateway вызывает sign (`d4ellill0lftr0somfne`), теперь через тег `production-stable`.
Создание новых версий не переключает Gateway автоматически.
`release-20260922` указывает на выпущенную версию,
`rollback-20260922` — на прежнюю `d4ejvpekbv3p3fn7ep8h`.

API key, очередь и реквизиты подписи сохранены. Storage-клиент использует
испытанные реквизиты bukupl. Новые S3-команды разрешены только для бакета
`1bf11148-3595-4a07-a089-d460153b7c7a`; в рабочем API снят WRITE_PREFIX.
Рабочая bukupl и IAM не менялись.

## Завершение тестового этапа

Пользователь подтвердил успешную подпись и выгрузку четырёх документов после
перехода (utilisation, agg, introduce), а также отсутствие других клиентов
тестового URL. 22.09.2026 в тестовой функции опубликована версия
`d4epkc9egcoeunb8ou1u` с DISABLE_QUEUE=true (операция `d4e245rmemfp1aqpntmg`).
Проверено: GetQueueAttributes тестового API возвращает 403 с причиной
Queue operations disabled in storage test environment; основной Gateway
возвращает 200 для capabilities v2 и GetQueueAttributes.
Сообщения при проверке не извлекались и не удалялись.
Сам тестовый API сохранён: S3-чтение доступно с ключом, запись ограничена
_api-tests/20260918/. Это блокировка команд очереди в коде, не отзыв S3/SQS-ключей.

## Проверки

- 25 тестов интерфейса, 20 SignJS, 5 API — успешно.
- Кандидат до переключения: capabilities, список S3, GetQueueAttributes
  без извлечения сообщений. Те же проверки через Gateway после переключения.
- Без ключа и с неверным ключом: 403.
- CORS OPTIONS: 204, origin https://storage.yandexcloud.net.
- Задания/: 623 объекта, 2 страницы; 2.061 и 0.758 секунды.
- allowed_delete_tags сохранён: error, processed.
- Общие файлы SignJS совпадают с испытанными; не изменялись.
- Опубликованный index.html проверен HTTP-скачиванием и SHA-256:
  `4f621b07e63abfa70d768d811735b28f258646b82902a14bde0f5297bf071470`.
- Новый JS: refresh-controller.v1.3.1.js. Старый refresh-controller.js сохранён.

Download различает ошибку получения ссылки и ошибку записи тега. Сообщение
«скачивание запущено» не утверждает, что браузер завершил скачивание.
Страница предлагает основной Gateway новым пользователям, но не перезаписывает
сохранённый URL. С тестового URL пользователь переключается вручную.

На тестовой странице пользователь ранее подтвердил подпись, выгрузку,
DeleteMessage (httpStatusCode 200, attempts 1), пагинацию, просмотр файлов,
удаление тега без перезагрузки. Полный цикл на основных адресах после
переключения браузера подтверждён пользователем по подписи и выгрузке;
DeleteMessage отдельно проверялся ранее на тестовой версии.

## Откат

Резервная копия старого index.html: локально `release-20260922/index-before.html`
и S3 `_release-backups/20260922/index.html` (создана с ACL private).
Локально сохранены исходная/закреплённая спецификации Gateway, id операции,
активная версия и хеши UI; секретов в этих файлах нет.

Из каталога ymq-sig, при необходимости отката:

```sh
node release-ui.cjs rollback
python3 release-production.py rollback
```

Сначала старый UI, затем прежний API. Rollback API меняет тег Gateway, но
НЕ системный $latest прямого URL функции. Исходную спецификацию без тега
не применять: она направила бы Gateway на новый $latest.
Подписи и обработанные сообщения откат не отменяет. Тестовую функцию не
отключать до подтверждения перехода всех активных подписывающих клиентов.
